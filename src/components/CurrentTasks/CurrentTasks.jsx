import { useState } from 'react'
import { cls } from './CurrentTasks.module.scss'



function CurrentTasks() {
    const { container } = cls;
    return (
        <secction className={container}>
            test
        </secction>
    )
}

export default CurrentTasks
